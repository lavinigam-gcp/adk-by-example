# ============================================================================
# ADK by Example - Environment Configuration
# ============================================================================
# Copy this file to .env and add your actual API keys
# All examples will use these environment variables
# ============================================================================

# ----------------------------------------------------------------------------
# PRIMARY: Gemini Models (REQUIRED for most examples)
# ----------------------------------------------------------------------------

# Option 1: Google AI Studio (Recommended for getting started)
# Get your free API key at: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your-google-api-key-here

# Option 2: Vertex AI (For production use)
# Requires Google Cloud project with billing enabled
# GOOGLE_CLOUD_PROJECT=your-project-id
# GOOGLE_CLOUD_LOCATION=us-central1
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# ----------------------------------------------------------------------------
# ALTERNATIVE MODELS (Only for specific integration examples)
# ----------------------------------------------------------------------------

# Anthropic Claude (for use-claude example)
# Get API key at: https://console.anthropic.com/
# ANTHROPIC_API_KEY=your-anthropic-key

# OpenAI (for OpenAI integration examples)
# Get API key at: https://platform.openai.com/api-keys
# OPENAI_API_KEY=your-openai-key

# ----------------------------------------------------------------------------
# TOOL INTEGRATIONS (For specific capability examples)
# ----------------------------------------------------------------------------

# Google Search (for search-google example)
# Get API key at: https://developers.google.com/custom-search/v1/introduction
# GOOGLE_SEARCH_API_KEY=your-search-api-key
# GOOGLE_SEARCH_ENGINE_ID=your-search-engine-id

# OAuth 2.0 (for oauth-integration examples)
# Create OAuth app in Google Cloud Console
# OAUTH_CLIENT_ID=your-oauth-client-id
# OAUTH_CLIENT_SECRET=your-oauth-client-secret
# OAUTH_REDIRECT_URI=http://localhost:8080/oauth/callback

# ----------------------------------------------------------------------------
# DATABASE CONNECTIONS (For data access examples)
# ----------------------------------------------------------------------------

# BigQuery (for query-bigquery example)
# Requires Google Cloud project with BigQuery enabled
# BIGQUERY_PROJECT=your-bigquery-project
# BIGQUERY_DATASET=your-dataset-name

# Firestore (for persist-to-firestore example)
# Requires Google Cloud project with Firestore enabled
# FIRESTORE_PROJECT=your-firestore-project
# FIRESTORE_COLLECTION=agents-data

# Spanner (for query-spanner example)
# SPANNER_INSTANCE=your-instance-id
# SPANNER_DATABASE=your-database-id

# ----------------------------------------------------------------------------
# DEPLOYMENT & MONITORING (For production examples)
# ----------------------------------------------------------------------------

# Cloud Run deployment
# CLOUD_RUN_SERVICE_NAME=adk-agent-service
# CLOUD_RUN_REGION=us-central1

# Monitoring & Telemetry
# ENABLE_TELEMETRY=true
# TELEMETRY_ENDPOINT=https://your-telemetry-endpoint.com

# Rate Limiting
# RATE_LIMIT_REQUESTS_PER_MINUTE=60
# RATE_LIMIT_TOKENS_PER_MINUTE=10000

# ----------------------------------------------------------------------------
# EXTERNAL SERVICES (For integration examples)
# ----------------------------------------------------------------------------

# GitHub (for github-integration example)
# GITHUB_TOKEN=your-github-personal-access-token

# Slack (for slack-bot example)
# SLACK_BOT_TOKEN=xoxb-your-bot-token
# SLACK_APP_TOKEN=xapp-your-app-token

# Custom API (for call-rest-api example)
# API_BASE_URL=https://api.example.com
# API_KEY=your-api-key

# ----------------------------------------------------------------------------
# LOCAL DEVELOPMENT (For offline examples)
# ----------------------------------------------------------------------------

# Ollama (for local-ollama example)
# OLLAMA_HOST=http://localhost:11434

# Local file paths
# LOCAL_DATA_PATH=/path/to/local/data
# LOCAL_CACHE_PATH=/tmp/adk-cache

# ----------------------------------------------------------------------------
# OPTIONAL: Example-specific settings
# ----------------------------------------------------------------------------

# Temperature settings for different use cases
# CREATIVE_TEMPERATURE=0.9
# BALANCED_TEMPERATURE=0.7
# PRECISE_TEMPERATURE=0.3

# Token limits
# MAX_OUTPUT_TOKENS=2048
# MAX_INPUT_TOKENS=8192

# ----------------------------------------------------------------------------
# NOTES:
# ----------------------------------------------------------------------------
# 1. Start with just GOOGLE_API_KEY to run most examples
# 2. Add other keys as needed for specific examples
# 3. Never commit your actual .env file to git
# 4. Keep your API keys secure and rotate them regularly
# 5. Use different keys for development and production
# ============================================================================